<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userSQL">
	
	<resultMap type="user.bean.UserDTO" id="userResult"></resultMap>
	
	<insert id="write" parameterType="user.bean.UserDTO">
		insert into usertable values(#{name}, #{id}, #{pwd})
	</insert> <!-- 넘어오는 데이터의 자료형-->
	<!--<select id="getUserList" resultType="user.bean.UserDTO">--><!--같은 이름을 찾아서 매핑을 한다.-->
	<!--	select * from usertable
	</select>-->
	
	<!-- 만약 UserDTO의 필드명과 오라클 컬럼명이 틀린 경우-->
</mapper>